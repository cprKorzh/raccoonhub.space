import React from 'react';
import manActive from './image/man-active.svg';

const ManActive = () => {
    return (
        <img width={40} src={manActive} alt="Man Active" />
    );
};

export default ManActive;
